const React = require("react-native");

const { StyleSheet } = React;

export default {
  container: {
    backgroundColor: "#fff"
  },
  imageContainer: {
    flex: 1,
    width: null,
    height: null
  }
};
